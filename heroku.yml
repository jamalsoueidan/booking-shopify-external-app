setup:
  config:
    env_file: .env
    NODE_ENV: test2
    env:
      NODE_ENV: test2
    environment:
      NODE_ENV: test3
    args:
      NODE_ENV: test4

build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: test1
