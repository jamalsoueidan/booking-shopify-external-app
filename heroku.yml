setup:
  config:
    env:
      NODE_ENV: ${{secrets.NODE_ENV}}
    environment:
      NODE_ENV: ${{secrets.NODE_ENV}}
    args:
      NODE_ENV: ${{secrets.NODE_ENV}}

build:
  docker:
    web: Dockerfile
