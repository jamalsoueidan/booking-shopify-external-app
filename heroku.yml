setup:
  config:
    env_file: .env
    NODE_ENV: ${{secrets.NODE_ENV}}
    env:
      NODE_ENV: ${{secrets.NODE_ENV}}
    environment:
      NODE_ENV: ${{secrets.NODE_ENV}}
    args:
      NODE_ENV: ${{secrets.NODE_ENV}}

build:
  docker:
    web: Dockerfile
