build:
  args:
    NODE_ENV: ${{secrets.NODE_ENV}}
  docker:
    web: Dockerfile
    env:
      NODE_ENV: ${{secrets.NODE_ENV}}
    environment:
      NODE_ENV: ${{secrets.NODE_ENV}}
    args:
      NODE_ENV: ${{secrets.NODE_ENV}}
